'use strict';

import SectionExamples from '../section-examples';

describe('SectionExamples View', function() {

  beforeEach(() => {
    this.sectionExamples = new SectionExamples();
  });

  it('Should run a few assertions', () => {
    expect(this.sectionExamples);
  });

});
